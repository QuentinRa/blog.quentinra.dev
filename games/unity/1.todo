-----------------------------------------------------------------
------------------------- Game Objects
-----------------------------------------------------------------

3D GO using Rigidbody, one unit = one meter

Z-fighting
=> two items at the same location, overlapping
=> bug when rendering
=> trick: increase the z-values by 0.01

2D GO Rigidbody, base sprite size one meter and one unit.
For example 32px = 1 meter and 1 unit. (Pixel Per Unit)

Z-axis rendering
=> layers